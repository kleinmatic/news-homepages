# Categories

Sites are grouped into {{ bundle_list|length }} bundles based on location, ideology and topic.

| Bundle  |
| :-----  |
{% for obj in bundle_list -%}
|[{{ obj.name }}](https://palewi.re/docs/news-homepages/bundles/{{ obj.slug }}.html)|
{% endfor %}
